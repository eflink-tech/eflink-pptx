/** a:txBody → 富文本（Task 4 实现完整版；当前占位以保证 Task 3 独立提交） */
import type { PreviewCtx, Box } from './svg'

export async function renderText(txBody: Element, ctx: PreviewCtx, box: Box): Promise<SVGElement | null> {
  void txBody
  void ctx
  void box
  return null
}
